{{> partials/header}}
<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-12">
            <div class="app-card p-4 p-md-5 text-dark">
                <div class="d-flex justify-content-between align-items-center mb-4">
                    <h2 class="fw-bold mb-0">Preguntas Reportadas</h2>
                </div>
                
                <div class="alert alert-warning mb-4">
                    <i class="bi bi-exclamation-triangle-fill me-2"></i>
                    Estas preguntas han sido reportadas por los usuarios más de 3 veces.
                </div>

                <div class="table-responsive">
                    <table class="table table-hover align-middle">
                        <thead class="table-light">
                            <tr>
                                <th scope="col">ID</th>
                                <th scope="col">Pregunta</th>
                                <th scope="col">Categoría</th>
                                <th scope="col" class="text-center">Reportes</th>
                                <th scope="col" class="text-end">Acciones</th>
                            </tr>
                        </thead>
                        <tbody>
                            {{#questions}}
                            <tr>
                                <td><span class="badge bg-secondary">{{question_id}}</span></td>
                                <td class="text-truncate" style="max-width: 300px;" title="{{question_text}}">{{question_text}}</td>
                                <td><span class="badge bg-info text-dark">{{category_name}}</span></td>
                                <td class="text-center"><span class="badge bg-danger rounded-pill">{{report_count}}</span></td>
                                <td class="text-end">
                                    <div class="btn-group" role="group">
                                        <a href="/editor/editQuestion?id={{question_id}}" class="btn btn-sm btn-outline-primary" title="Editar Pregunta">Editar</a>
                                        <a href="/editor/dismissReports?id={{question_id}}" class="btn btn-sm btn-outline-warning" title="Ignorar Reportes" onclick="return confirm('¿Estás seguro de ignorar los reportes? Se borrarán todos los reportes de esta pregunta.')">Ignorar</a>
                                        <a href="/editor/deleteQuestion?id={{question_id}}" class="btn btn-sm btn-outline-danger" title="Eliminar Pregunta" onclick="return confirm('¿Estás seguro de eliminar esta pregunta?')">Eliminar</a>
                                    </div>
                                </td>
                            </tr>
                            {{/questions}}
                            {{^questions}}
                            <tr>
                                <td colspan="5" class="text-center py-4 text-muted">
                                    <i class="bi bi-check-circle-fill text-success fs-4 d-block mb-2"></i>
                                    No hay preguntas reportadas pendientes.
                                </td>
                            </tr>
                            {{/questions}}
                        </tbody>
                    </table>
                </div>
                
                <div class="mt-4">
                    <a href="/editor/dashboard" class="btn btn-outline-secondary">
                        <i class="bi bi-arrow-left me-1"></i>Volver al Panel
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>
{{> partials/footer}}
