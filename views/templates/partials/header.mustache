<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-sRIl4kxILFvY47J16cr9ZwB07vP4J8+LH7qKQnuqkuIAvNWLzeN8tE5YBujZqJLB" crossorigin="anonymous">
    <title>Preguntados</title>
    <link rel="stylesheet" href="../../assets/css/roulette.css">
    <link rel="stylesheet" href="../../assets/css/roulette-rotations.css">
    <link rel="stylesheet" href="../../assets/css/roulette-colors.css">
    <style>
        body {
            min-height: 100vh;
            background: linear-gradient(135deg, #f97316 0%, #ef4444 100%);
            display: flex;
            justify-content: center;
        }

        .navegador {
            display: flex;
            justify-content: center;
            column-gap: 5px;
        }

        .card {
            box-shadow: 0 15px 35px rgba(0, 0, 0, 0.2);
            border: none;
            border-radius: 16px;
        }

        .container {
            margin-top: 50px;
        }

        table img {
            width: 60px;
            height: 60px;
            object-fit: cover;
            border-radius: 50%;
        }

        .table-responsive {
            box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.1);
            background-color: white;
            border-radius: 10px;
            padding: 20px;
        }

        h1 {
            text-align: center;
            margin-bottom: 30px;
            font-weight: bold;
        }

        /*ruleta*/
        .wheel {
            position: relative;
            width: 300px;
            height: 300px;
            margin: 50px auto;
            border-radius: 50%;
            background: -webkit-linear-gradient(-90deg, #f9dd62, #ec4e20);
            background: linear-gradient(-90deg, #f9dd62, #ec4e20);
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.3);
        }
        .number {
            position: absolute;
            width: 60px;
            height: 60px;
            font: bold 20px Arial, sans-serif;
            color: #fff;
            text-align: center;
            line-height: 60px;
            border-radius: 50%;
            background-color: #333;
            transform-origin: 50% 205px;
            animation: spin 3s ease-out;
        }
        @keyframes spin {
            from { transform: rotate(0deg); }
            to { transform: rotate(360deg); }
        }
        /*ruleta*/
    </style>
</head>

<body class="d-flex flex-column min-vh-100 bg-dark text-white">
    <header class="bg-primary p-3 mb-4">
        <h1>Preguntados</h1>
        <nav class="navegador">
            <a class="text-decoration-none text-white" href="/">Inicio</a> |
            <a class="text-decoration-none text-white" href="/login/login">Inicio de sesión</a> |
            <a class="text-decoration-none text-white" href="/login/registro">Registro</a>
            {{#usuario}}
                | <a class="text-decoration-none text-white" href="/login/logout">Cerrar sesión ({{usuario}})</a>
            {{/usuario}}
        </nav>
    </header>

