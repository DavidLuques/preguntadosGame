<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Preguntados</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-sRIl4kxILFvY47J16cr9ZwB07vP4J8+LH7qKQnuqkuIAvNWLzeN8tE5YBujZqJLB" crossorigin="anonymous">
    <link rel="stylesheet" href="../../assets/css/roulette.css">
    <link rel="stylesheet" href="../../assets/css/roulette-rotations.css">
    <link rel="stylesheet" href="../../assets/css/roulette-colors.css">
    <style>
        :root {
            --gradient-start: #f97316;
            --gradient-end: #ec4899;
            --card-bg: rgba(255, 255, 255, 0.96);
        }

        body {
            min-height: 100vh;
            background: radial-gradient(circle at top, rgba(255, 255, 255, 0.1), transparent 60%), linear-gradient(135deg, var(--gradient-start), var(--gradient-end));
            font-family: "Inter", system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
        }

        .app-card {
            border: none;
            border-radius: 1.25rem;
            background-color: var(--card-bg);
            box-shadow: 0 20px 45px rgba(15, 23, 42, 0.25);
        }

        .table-avatar img {
            width: 56px;
            height: 56px;
            object-fit: cover;
            border-radius: 50%;
            border: 3px solid rgba(255, 255, 255, 0.9);
            box-shadow: 0 6px 18px rgba(15, 23, 42, 0.25);
        }

        .section-title {
            font-size: 1.5rem;
            font-weight: 700;
            color: #0f172a;
        }

        .subtext {
            color: #475569;
        }

        .roulette-card {
            background: linear-gradient(160deg, rgba(15, 23, 42, 0.95), rgba(59, 7, 100, 0.95));
            color: #f8fafc;
        }

        footer {
            background: rgba(15, 23, 42, 0.85);
        }

        .roulette-border {
            border: 6px solid rgba(255, 255, 255, 0.15);
            border-radius: 50%;
            padding: 1.25rem;
            backdrop-filter: blur(4px);
        }

        .roulette-container .button-container button {
            min-width: 130px;
        }
    </style>
</head>

<body class="min-vh-100 d-flex flex-column text-dark">
    <header class="shadow-sm">
        <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
            <div class="container">
                <a class="navbar-brand fw-semibold" href="/">🎯 Preguntados</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#mainNav" aria-controls="mainNav" aria-expanded="false" aria-label="Mostrar navegación">
                    <span class="navbar-toggler-icon"></span>
                </button>

                <div class="collapse navbar-collapse" id="mainNav">
                    <ul class="navbar-nav ms-auto gap-lg-3 align-items-lg-center">
                        <li class="nav-item">
                            <a class="nav-link" href="/">Inicio</a>
                        </li>
                        {{^usuario}}
                            <li class="nav-item">
                                <a class="nav-link" href="/login/login">Iniciar sesión</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="/login/registro">Registrarme</a>
                            </li>
                        {{/usuario}}
                        {{#usuario}}
                            <li class="nav-item">
                                <a class="nav-link text-white fw-bold" href="/perfil/perfil">Mi Perfil</a>
                            </li>
                            <li class="nav-item">
                                <a class="btn btn-sm btn-light text-primary fw-semibold" href="/login/logout">Salir ({{usuario}})</a>
                            </li>
                        {{/usuario}}
                    </ul>
                </div>
            </div>
        </nav>
    </header>

    <main class="flex-grow-1 py-5">

